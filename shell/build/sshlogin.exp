#!/usr/bin/expect -f
set command [lrange $argv 0 0]
set password [lrange $argv 1 1]
set timeout -1
spawn ssh-copy-id -i /root/.ssh/id_rsa.pub $command 
match_max 100000
expect "*?assword:*"
send -- "$password\r"
send -- "\r"
expect eof
